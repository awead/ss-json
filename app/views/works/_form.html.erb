<%= form_with(model: work, local: true) do |form| %>
  <% if work.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(work.errors.count, "error") %> prohibited this work from being saved:</h2>

      <ul>
      <% work.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.fields_for(:work_versions, work.work_versions.first) do |work_versions_form| %>
    <%= render partial: 'work_versions/form_fields', locals: { form: work_versions_form } %>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
