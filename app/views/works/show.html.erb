<p id="notice"><%= notice %></p>

<h1>Latest Version</h1>

<span class="badge badge-primary"><%= @work.latest_version.aasm_state %></span>
<%= render partial: 'work_versions/show_fields', locals: { work_version: @work.versions.last } %>

<%= link_to 'Back', works_path %> |
<%= link_to 'New Version', new_work_version_path(@work) %> |
<%= link_to 'All Versions', work_versions_path(@work) %>
