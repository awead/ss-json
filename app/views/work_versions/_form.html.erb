<% if work_version.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(work_version.errors.count, "error") %> prohibited this work_version from being saved:</h2>

    <ul>
    <% work_version.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<%= render 'form_fields', form: form %>

<div class="actions">
  <%= link_to "Back", work_version_file_list_path(@work, @work_version), class: 'btn btn-secondary' %>
  <%= form.submit "Save and Continue", class: 'btn btn-primary' %>
</div>
